//this function handles all click events on the document
let click = (evt)=>{
  /*click on circles*/
  if (evt.target.className === "circles") {
    // IDEA: circles "drop down" and bounce up again
    //evt.target.style.
  }
}
